# How Vercel chooses pnpm
# Pin version via package.json "packageManager" (pnpm@10.15.1).
# Also set in Vercel → Settings → Environment Variables:
#   ENABLE_EXPERIMENTAL_COREPACK=1
# so Corepack uses that exact version instead of an older default.
engine-strict=false
