# Not a multi-package monorepo — root app only.
# allowBuilds: required so CI/Vercel can run native install scripts (Next.js → sharp).
packages:
  - "."

allowBuilds:
  sharp: true
  unrs-resolver: true
